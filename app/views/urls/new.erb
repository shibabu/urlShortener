<h1>urlShortener New</h1>

<% if @url.errors.any? %>
  <% @url.errors.full_messages.each do |msg| %>
    <%= msg %>
  <% end %>
<% end %>

<%= form_for @url do |f| %>
  <%= f.label :url %> : <%= f.text_field :original %>
  <%= f.submit 'urlShorten' %>
<% end %>